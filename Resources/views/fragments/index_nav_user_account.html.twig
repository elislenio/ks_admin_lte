<li class="dropdown user user-menu">
	<a href="#" class="dropdown-toggle" data-toggle="dropdown">
	  <img src="{{ theme.user_image(app.user.picture) }}" class="user-image" alt="User Image">
	  <span class="hidden-xs">{{ app.user.username }}</span>
	</a>
	<ul class="dropdown-menu">
	  <!-- User image -->
	  <li class="user-header">
		<img src="{{ theme.user_image(app.user.picture) }}" class="img-circle" alt="User Image">
		<p>
		  {{ app.user.username }}
		  <!--<small>Member since Nov. 2012</small>-->
		</p>
	  </li>
	  <!-- Menu Body -->
	  <!--
	  <li class="user-body">
		<div class="col-xs-4 text-center">
		  <a href="#">Followers</a>
		</div>
		<div class="col-xs-4 text-center">
		  <a href="#">Sales</a>
		</div>
		<div class="col-xs-4 text-center">
		  <a href="#">Friends</a>
		</div>
	  </li>
	  -->
	  <!-- Menu Footer-->
	  <li class="user-footer">
		{% if ac.localPasswordEnabled() %}
		{% set logout_position = 'class=pull-right' %}
		<div class="pull-left">
		  <a href="{{ path('pwdselfchange') }}" class="btn btn-default btn-flat">Cambiar contraseña</a>
		</div>
		{% else %}
			{% set logout_position = 'style=text-align:center;' %}
		{% endif %}
		<div {{ logout_position }}>
		  <a href="{{ path('logout') }}" class="btn btn-default btn-flat">Cerrar Sesión</a>
		</div>
	  </li>
	</ul>
  </li>